# Railway deployment — server + host display

[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "npm run start --workspace=server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
